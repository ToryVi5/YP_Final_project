# Созадала 1 курьера в Postman (/api/v1/courier):
{
    "login": "ToryVi5",
    "password": "1234",
    "firstName": "Tory"
}

# Далее создала 6 заказов в Postman (/api/v1/orders):
{
    "firstName": "Itachi",
    "lastName": "Uchiha",
    "address": "Vnutrenniy dvor, 142 apt.",
    "metroStation": 29,
    "phone": "+7 811 355 35 35",
    "rentTime": 3,
    "deliveryDate": "2026-06-08",
    "comment": "Hallo, it's me!",
    "color": [
        "BLACK"
    ]
}

# С помощью запросов - принять заказ (/api/v1/orders/accept/1?courierId=1);
# отменить заказ (/api/v1/orders/cancel?track=749612;
# завершить заказ (/api/v1/orders/finish/1?id=634983) - изменила статусы заказам.

# Запрос:
SELECT track,
       CASE
        WHEN "finished" THEN 2
        WHEN "cancelled" THEN -1
        WHEN "inDelivery" THEN 1
        ELSE 0
       END AS status
FROM "Orders"; 

# Пояснение:

# SELECT track: выводит трекеры заказов;
# Оператор CASE позволяет определить значение нового столбца status на основе условий:
# Если finished = true, то выводится статус 2 (заказ завершён);
# Если cancelled = true, то статус равен -1 (заказ отменён);
# Если "inDelivery" = true, то статус становится 1 (заказ в доставке);
# Для всех остальных случаев возвращается статус 0 (неопределённый или другой статус).